import React from 'react';
import { Container, TextField, Button, List, ListItem, ListItemText, Divider, Grid, } from '@mui/material';

const StudentMessage: React.FC = () => {
    return (
        <Grid container spacing={2} className='bg-[#f3f2f0] min-h-screen'>
            <Grid item xs={5} className='border-4 m-8'>
                abc
            </Grid>
            <Grid item xs={7}>
                abc
            </Grid>
        </Grid>

    );
};


export default StudentMessage;